SELECT ?Domain ?DatasetLabel ?Structure ?defClass
WHERE{
sdtmig-3-1-3:Dataset.${Dataset}  mms:context               ?Context;
                                mms:contextName           ?Domain;
                                mms:contextLabel          ?DatasetLabel;
                                cdiscs:datasetStructure   ?Structure.
?Context                        mms:contextLabel          ?defClass.
}
